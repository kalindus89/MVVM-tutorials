<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mvvm_tutorials">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVVMTutorials">
        <activity
            android:name=".mvvm_movie_retrofit.MovieDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".mvvm_lesson_1.MvvmLesson_1_Activity"
            android:exported="false" />
        <activity
            android:name=".a_simple_binding.view.MainActivity"
            android:exported="false" />
        <activity
            android:name=".mvvm_movie_retrofit.ActivityMovieMain"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>